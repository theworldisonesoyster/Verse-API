---| Evaluate | 在指定时间 t 求缓动曲线值并返回。 |name: easing_function| Evaluate | 在指定时间 t 求缓动曲线值并返回。 |slug: versedotorg/scenegraph/keyframedmovement/easing_function| Evaluate | 在指定时间 t 求缓动曲线值并返回。 |url: https://dev.epicgames.com/documentation/fortnite/verse-api/versedotorg/scenegraph/keyframedmovement/easing_function| Evaluate | 在指定时间 t 求缓动曲线值并返回。 |kind: class| Evaluate | 在指定时间 t 求缓动曲线值并返回。 |module: /Verse.org/scenegraph/keyframedmovement| Evaluate | 在指定时间 t 求缓动曲线值并返回。 |grade: B| Evaluate | 在指定时间 t 求缓动曲线值并返回。 |depth: brief| Evaluate | 在指定时间 t 求缓动曲线值并返回。 |status: done| Evaluate | 在指定时间 t 求缓动曲线值并返回。 |---| Evaluate | 在指定时间 t 求缓动曲线值并返回。 || Evaluate | 在指定时间 t 求缓动曲线值并返回。 |# easing_function class <B>| Evaluate | 在指定时间 t 求缓动曲线值并返回。 || Evaluate | 在指定时间 t 求缓动曲线值并返回。 |> Base class for an animation easing function.| Evaluate | 在指定时间 t 求缓动曲线值并返回。 |> 动画缓动函数的基类。| Evaluate | 在指定时间 t 求缓动曲线值并返回。 || Evaluate | 在指定时间 t 求缓动曲线值并返回。 |`using { /Verse.org/SceneGraph/KeyframedMovement }`| Evaluate | 在指定时间 t 求缓动曲线值并返回。 || Evaluate | 在指定时间 t 求缓动曲线值并返回。 |## Members| Evaluate | 在指定时间 t 求缓动曲线值并返回。 || Evaluate | 在指定时间 t 求缓动曲线值并返回。 |只有函数，没有数据成员。| Evaluate | 在指定时间 t 求缓动曲线值并返回。 || Evaluate | 在指定时间 t 求缓动曲线值并返回。 |### Functions| Evaluate | 在指定时间 t 求缓动曲线值并返回。 || Function Name | Description || Evaluate | 在指定时间 t 求缓动曲线值并返回。 || Evaluate | 在指定时间 t 求缓动曲线值并返回。 || Evaluate | 在指定时间 t 求缓动曲线值并返回。 |