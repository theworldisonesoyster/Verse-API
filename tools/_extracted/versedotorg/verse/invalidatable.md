Implemented by classes whose instances can become invalid at runtime.
|  |  |
| Verse using statement | using { /Verse.org/Verse } |

## Exposed Interfaces
This interface exposes the following interfaces:
| Name | Description |
| disposable | Implemented by classes whose instances have limited lifetimes. |

## Members
This interface has functions, but no data members.

### Functions
| Function Name | Description |
| IsValid | Succeeds if this object is still valid. |
