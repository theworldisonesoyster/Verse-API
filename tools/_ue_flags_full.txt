CELL||Relative ordering among categories in the quest UI; lower values sort first.
CELL||The owning category that this category is considered nested under.
CELL||Signaled when a participant abandons this quest.
CELL||Signaled when this quest completes.
CELL||Signaled when a participant joins this quest.
CELL||Progression model for the quest.
CELL||Rewards granted to eligible participants on completion.
CELL||Marks this quest as completed. Fires CompleteEvent. Idempotent — no-op if already completed.
CELL||Succeeds if this quest is completed.
CELL||Replaces the progression model on a live quest. Auto-completes if the new objective is already completed.
CELL||Signaled when this objective's progress changes. Payload: this objective.
CELL||Returns a progress message (e.g. "3 / 10").
CELL||Determines which participants can contribute to this objective. Override to customize contributor selection. Default: all participants where Info.Contributes = true.
CELL||Succeeds if this objective is currently complete.
CELL||Signals ProgressEvent. Call from a subclass to notify progress subscribers.
CELL||Signaled after this reward has been granted to recipients.
CELL||Determines which participants should receive this reward. Override to customize recipient selection. Default: all participants where Info.Receives = true.
CELL||Grants this reward to the eligible participants.
CELL||Quantity per participant.
CELL||Iterates EligibleParticipants, resolves each participant's agent as a player, and grants the entitlement via the platform entitlement service.
CELL||Current progress toward RequiredCount.
CELL||Target count for completion.
CELL||Returns progress in "X / Y" format.
CELL||Succeeds if Progress >= RequiredCount.
CELL||Sets current progress. Clamped to [0, RequiredCount].
CELL||Sets the target count. Clamped to [1, MaxFloat].
CELL||The categories this quest is grouped under in the quest UI. Empty = uncategorized.
CELL||Items that were newly added to this inventory as a result of the transaction.
CELL||Items whose stack size changed as a result of the transaction, and the previous stack size value.
CELL||The inventory which the item was removed from.
CELL||Total stack count of items removed as a result of this transaction.
CELL||Items that were removed from this inventory as a result of the transaction.
CELL||Item whose stack size changed as a result of the transaction.
CELL||Triggered when an item is removed. Fires on this inventory, then propagates up to all ancestor inventories.
CELL||Triggered when an item is unequipped. Fires on this inventory, then propagates up to all ancestor inventories.
CELL||Adds the item to this inventory only, ignoring sub-inventories. Fails if no items could be added.
CELL||Adds the item to this inventory, including sub-inventories. Fails if no items could be added.
CELL||Returns a list of all sub-inventories within this inventory and its descendant inventories.
CELL||Returns a list of all items within this inventory and its descendant inventories.
CELL||Returns a list of all items (of a specific type) within this inventory and its descendant inventories.
CELL||Returns a list of sub-inventories of this inventory only.
CELL||Returns a list of items within this inventory only.
CELL||Returns a list of items (of a specific type) within this inventory only.
CELL||Removes an item from this inventory or its descendent inventories. Fails if the item could not be removed.
CELL||Broadcast when this item changes inventory.
CELL||Removes the item from its inventory and places it in the simulation world. Works on orphaned items i.e. outside the world or any inventory. Fails if the item is already a pickup. The transform of the item will not be altered except to unparent it from any inventory.
CELL||Removes the item from its inventory and places it in the simulation world. Works on orphaned items i.e. outside the world or any inventory. Fails if the item is already in the world. The transform of the dropped item will be set to WorldTransform.
CELL||Attempts to equip this item within the inventory it is currently in. Fails if not in an inventory or if equip_item_query_event contains any errors after querying.
CELL||Returns the inventory_component this item currently resides in. Fails if it cannot find a valid parent inventory.
CELL||Succeeds if this item is considered currently within an inventory and equipped.
CELL||Fails if this item is already in an inventory, or the inventory cannot accept the item.
CELL||Attempts to unequip this item. Fails if not in an inventory, not currently equipped or if unequip_item_query_event contains any errors after querying.
CELL||Retrieve an integer value or fail if value is not a json number
CELL||Retrieve a float value or fail if value is not a json number
CELL||Retrieve an object value or fail if value is not a string
CELL||Retrieve an object value or fail if value is not null
CELL||Used to render a mesh at the location of this entity. A mesh is a set of polygons that can be used to represent shapes in the world such as: foliage and terrain decorations movers (for example, doors and lifts) procedurally created buildings Dependencies: transform_component on the entity positions the mesh.
CELL||Enable/disable collision on this mesh. If enabled, meshes may collide in the physics simulation.
CELL||Triggered at the beginning of each tick when another entity first overlaps this entity.
CELL||Triggered at the beginning of each tick when another entity is no longer overlapping this entity
CELL||Enable/disable spatial queries against this mesh. Disabling this field will also disable EntityEnteredEvent/EntityExitedEvent.
CELL||Enable/disable visibility of this mesh.
CELL||Disables rendering of this mesh.
CELL||Enables rendering of this mesh.
CELL||Succeeds if the component is enabled, fails if it's disabled.
CELL||Adds the provided components to the entity. If a component is not allowed to be added to this entity it is skipped. Note: When called during the AddedToScene or BeginSimulation phase, it will make sure the added component has achieved the corresponding phase. Components are added following these rules: All components are added to the entity child list. All components have OnAddedToScene called (if this entity is in the scene). All components have OnBeginSimulation called (if this entity is simulating).
CELL||Adds the provided entities as children of this entity. If child entity already has a parent, removes the entity from its current parent and adds it to the new one. Added child entities will move through their lifetime methods until they match the state of the new parent.
CELL||Adds a tag instance to this entity. Returns a tag_key that is uniquely associated with the added instance.
CELL||Fails if at least one type in tag_types cannot be found in this container, succeeds otherwise. Note that this means that if tag_types is empty this call succeeds.
CELL||Succeeds if at least of the types in tag_types is found in this container, fails otherwise. Note that this means that if tag_types is empty this call fails.
CELL||Succeeds if at least one tag of type tag_type is found in this container, fails otherwise.
CELL||Succeeds and returns the child component of type component_type if it exists and is accessible from the calling context. Note: When called during the AddedToScene or BeginSimulation phase, it will make sure the returned component has achieved the corresponding phase. Fails if no component of component_type exists or can be accessed.
CELL||Returns the child components belonging to this entity which are accessible from the calling context.
CELL||Returns the child entities belonging to this entity which are accessible from the calling context. This method only gets the direct entity children. To query multiple levels down the entity structure use the Find* query methods instead.
CELL||Returns the parent entity of this entity. The parent entity controls the lifetime of its child entities and components. When an entity is removed from the scene, all its child entities and components will be removed as well. Method fails if there is currently no parent entity.
CELL||Removes all tag instances of type tag_type, succeeds if at least one instance was removed, fails otherwise.
CELL||Removes all tag instances that are not of type tag_type, succeeds if at least one instance was removed, fails otherwise.
CELL||Removes all tag instances that are not of any of the types in tag_types, succeeds if at least one instance was removed, fails otherwise.
CELL||Removes this entity from its parent. This is used to remove entities from the scene. Components on this entity and its children will run through OnEndSimulation -> OnRemovingFromScene. Entity can be added back later by using NewParent.AddEntities.
CELL||Removes the tag instance associated with the tag_key, succeeds if an instance was removed, fails otherwise.
CELL||Send a scene event to this entity and then down the hierarchy. First, SendDown/OnReceive will be invoked on each component on this entity. Next, SendDown will be invoked on each child entity. Consuming the event at any point will halt propagation. Returns true if any participant consumed the event.
CELL||Send a scene event to this entity and then up the hierarchy. First, SendDown/OnReceive will be invoked on each component on this entity. Next, SendUp will be invoked on this entity's parent. Consuming the event at any point will halt propagation. Returns true if any participant consumed the event.
CELL||Detection has started for this input e.g. a required key is now held down. Note: ActivationTriggeredEvent may also occur this frame, but this event will always be fired first. DetectionBegin and DetectionEnd will always fire, regardless of whether the input is successful or canceled. Tuple payload: 0: the player generating this event 1: the value generated by the physical input
CELL||Detection for this input is still being processed. For example, a time threshold may not be met. Tuple payload: 0: the player generating this event 1: the value generated by the physical input 2: elapsed seconds since detection began
CELL||Detection has finished e.g. no required keys are now down. Will always fire as a pair with DetectionBegin. Tuple payload: 0: the player generating this event 1: elapsed seconds since detection began
CELL||Adds Widget to this player_ui using default player_ui_slot configuration options.
CELL||Adds Widget to this player_ui using Slot for configuration options.
CELL||Removes Widget from this player_ui.
CELL||Sets the user's focus on this Widget. The target Widgetmust be focusable, otherwise this has no effect. If SetFocus is called before AddWidget, the widget will be focused after AddWidget is called, unless a SetFocus is called on a different widget by the time AddWidget is called.
CELL||Shows or hides the widget without removing itself from the containing player_ui. See widget_visibility for details.
CELL||Returns the current widget_visibility state.
CELL||Enables or disables whether the player can interact with this widget.
CELL||true if this widget can be modified interactively by the player.
CELL||Returns the widget's parent widget. Fails if no parent exists, such as if this widget is not in the player_ui or is itself the root widget.
CELL||Returns the widget that added this widget to the player_ui. The root widget will return itself. Fails if this widget is not in the player_ui.
CELL||Controls widget input event consumption.
CELL||The player that triggered the event.
CELL||The widget that triggered the event.
CELL||Holds the minimum anchors, (left, top). The valid range is between 0.0 and 1.0.
CELL||Holds the maximum anchors, (right, bottom). The valid range is between 0.0 and 1.0.
CELL||The left edge spacing.
CELL||The top edge spacing.
CELL||The right edge spacing.
CELL||The bottom edge spacing.
CELL||Base class for all UI elements drawn on the player's screen.
CELL||The child widget of the button. Used only during initialization of the widget and not modified by SetSlot.
CELL||The UI input action that will trigger the Click event of this button.
CELL||Subscribable event that fires when the button is clicked.
CELL||Sets the child widget slot.
CELL||The widget assigned to this slot.
CELL||Horizontal alignment of the widget inside the slot.
CELL||Vertical alignment of the widget inside the slot.
CELL||Empty distance in pixels that surrounds the widget inside the slot. Assumes 1080p resolution.
CELL||The child widgets of the canvas. Used only during initialization of the widget and not modified by Add/RemoveWidget.
CELL||Adds a new child slot to the canvas.
CELL||Removes a slot containing the given widget.
CELL||The border for the margin and how the widget is resized with its parent. Values are defined between 0.0 and 1.0.
CELL||The offset that defined the size and position of the widget. When the anchors are well defined, the Offsets.Left represent the distance in pixels from the Anchors Minimum.X, the Offsets.Bottom represent the distance in pixel from the Anchors Maximum.Y, effectively controlling the desired widget size. When the anchors are not well defined, the Offsets.Left and Offsets.Top represent the widget position and Offsets.Right and Offset.Bottom represent the widget size.
CELL||When true we use the widget's desired size. The size calculated by the Offsets is ignored.
CELL||Alignment is the pivot/origin point of the widget. Starting in the upper left at (0.0,0.0), ending in the lower right at (1.0,1.0).
CELL||Z Order of this slot relative to other slots in this canvas panel. Higher values are rendered last (and so they will appear to be on top)
CELL||The color of the widget. Used only during initialization of the widget and not modified by SetColor.
CELL||The size this widget desired to be displayed in. Used only during initialization of the widget and not modified by SetDesiredSize.
CELL||The opacity of the widget. Used only during initialization of the widget and not modified by SetOpacity.
CELL||Gets the widget's color.
CELL||Gets the size this widget desired to be displayed in.
CELL||Gets the widget's opacity.
CELL||Sets the widget's color.
CELL||Sets the size this widget desired to be displayed in.
CELL||Sets the widgets's opacity.
CELL||The horizontal tiling option. Used only during initialization of the widget and not modified by SetTiling.
CELL||The image to render. Used only during initialization of the widget and not modified by SetImage.
CELL||Tinting applied to the image. Used only during initialization of the widget and not modified by SetTint.
CELL||The vertical tiling option. Used only during initialization of the widget and not modified by SetTiling.
CELL||Gets the image to render.
CELL||Gets the tiling option.
CELL||Gets the tint applied to the image.
CELL||Sets the image to render.
CELL||Sets the tiling option when the image is smaller than the allocated size.
CELL||Sets the tint applied to the image.
CELL||The child widgets of the overlay. Used only during initialization of the widget and not modified by Add/RemoveWidget.
CELL||Add a new child slot to the overlay. Slots are added at the end.
CELL||Removes a slot containing the given widget
CELL||Horizontal alignment of the widget inside the slot. This alignment is only applied after the layout space for the widget slot is created and determines the widget alignment within that space.
CELL||Vertical alignment of the widget inside the slot. This alignment is only applied after the layout space for the widget slot is created and determines the widget alignment within that space.
CELL||The orientation of the stack box. Either stack widgets horizontal or vertical.
CELL||The child widgets of the stack box. Used only during initialization of the widget and not modified by Add/RemoveWidget.
CELL||Add a new child slot to the stack box. Slots are added at the end.
CELL||The available space will be distributed proportionally. If not set, the slot will use the desired size of the widget.
CELL||Whether the text should be automatically wrapped.
CELL||The justification to display to the user. Used only during initialization of the widget and not modified by SetJustification.
CELL||The policy that determine what happens when the text is longer than its allowed length. Used only during initialization of the widget and not modified by SetOverflowPolicy.
CELL||The text to display to the user. Used only during initialization of the widget and not modified by SetText.
CELL||The color of the displayed text. Used only during initialization of the widget and not modified by SetTextColor.
CELL||The opacity of the displayed text. Used only during initialization of the widget and not modified by SetTextOpacity.
CELL||The size of the displayed text. Used only during initialization of the widget and not modified by SetTextSize.
CELL||The wrapping policy to determine where the line can be broken.
CELL||Whether text wraps onto a new line when it's length exceeds this width; if this value is zero or negative, no wrapping occurs.
CELL||Gets the text justification in the widget.
CELL||Gets the policy that determine what happens when the text is longer than its allowed length.
CELL||Gets the text currently in the widget.
CELL||Gets the color of the displayed text.
CELL||Gets the opacity of the displayed text.
CELL||Gets the size of the displayed text.
CELL||Sets the text justification in the widget.
CELL||Sets the policy that determine what happens when the text is longer than its allowed length.
CELL||Sets the text displayed in the widget.
CELL||Sets the color of the displayed text.
CELL||Sets the opacity of the displayed text.
CELL||Sets the size of the displayed text.
CELL||widget does not consume any input.
CELL||widget consumes all inputs
CELL||The widget is visible and occupies layout space.
CELL||The widget is invisible and does not occupy layout space.
CELL||The widget is invisible and occupies layout space.
CELL||Orient widgets from left to right.
CELL||Orient widgets from top to bottom.
CELL||Center widget horizontally within the slot.
CELL||Align widget to the left of the slot.
CELL||Align widget to the right of the slot.
CELL||widget fills the slot horizontally.
CELL||Center widget vertically within the slot.
CELL||Align widget to the top of the slot.
CELL||Align widget to the bottom of the slot.
CELL||widget fills the slot vertically.
CELL||Stretch the image to fit the available space.
CELL||Repeat/Wrap the image to fill the available space.
CELL||Evaluates this float curve at the specified time and returns the result as a float
CELL||Channel will be used to clear specific debug draw.
CELL||Show Debug Draw for the channel for all users.
CELL||Hide Debug Draw for the channel for all users.
CELL||Clears all debug draw for the channel.
CELL||Clears all debug draw from this debug_draw instance.
CELL||Draws a sphere at the named location, and using the provided draw parameters.
CELL||Draws a box at the named location, and using the provided draw parameters
CELL||Draws a capsule at the named location, and using the provided draw parameters.
CELL||Draws a cone at the named location, and using the provided draw parameters.
CELL||Draws a cylinder at the named location, and using the provided draw parameters.
CELL||Draws a line from Start to End locations, and using the provided draw parameters.
CELL||Draws a point at the named location, and using the provided draw parameters.
CELL||Draws an arrow pointing from Start to End locations, and using the provided draw parameters.
CELL||Draws a 3D text using the provided draw parameters.
CELL||Channel class name will be added as a prefix used when printing the message e.g. '[log_channel]: #Message
CELL||Sets the default log level of the displayed message. See log_level enum for more info on log levels. Defaults to log_level.Normal.
CELL||Print Message using the given log level.
CELL||Print Message diagnostic using the given log level.
CELL||Prints the current script call stack using the given log level.
